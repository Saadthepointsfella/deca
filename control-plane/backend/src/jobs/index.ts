// backend/src/jobs/index.ts
// Placeholder for background workers (BullMQ / cron).
// For now, you can skip running this and come back later.

export async function startJobsWorker(): Promise<void> {
  // TODO: wire queue/cron to run usageAggregatesJob, slaMonitorJob
}
